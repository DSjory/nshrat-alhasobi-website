Place the GitHub SVG here using the exact filename you provided: `GitHub_Invertocat_Black_Clearspace.svg`.

Save the provided SVG (unchanged) to:

assets/icons/GitHub_Invertocat_Black_Clearspace.svg

Usage:
- `style.css` uses this SVG as a mask (`.icon.icon-github`) and colors it with `var(--secondary-color)` (#043E52). The SVG file itself is not modified.
- `about.html` developer cards use `<span class="icon icon-github"></span>` so styling comes entirely from CSS.

If you previously saved a PNG, you may remove it — the site now uses the supplied SVG for crispness and CSS coloring.

Also add the LinkedIn SVG (unchanged) as:

assets/icons/linkedin.svg

`style.css` will use this file as a mask for `.icon.icon-linkedin` and color it using `var(--secondary-color)` (#043E52).